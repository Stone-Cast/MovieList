import SectionName from "./SectionName";

const Cast = () => {
    return (
        <section className="md:mt-3">
            <SectionName name="cast" />
            <div></div>
        </section>
    );
};

export default Cast;
